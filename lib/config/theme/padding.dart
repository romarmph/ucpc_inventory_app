import 'package:flutter/material.dart';

class AppPaddings {
  static const scaffoldPadding = EdgeInsets.all(16.0);
  static const buttonPadding = EdgeInsets.symmetric(
    vertical: 24,
    horizontal: 24,
  );
}
